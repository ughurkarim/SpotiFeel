{
  "name": "spotifeel",
  "version": "1.0.0",
  "description": "SpotiFeel - Music visualization and analytics application",
  "private": true,
  "scripts": {
    "dev": "echo 'Development server runs via Flask'",
    "build": "echo 'Static files served directly'",
    "lint": "echo 'No linter configured yet'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  },
  "engines": {
    "node": ">=14"
  }
}
